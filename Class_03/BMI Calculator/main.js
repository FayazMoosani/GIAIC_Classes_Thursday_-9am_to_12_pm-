// Revised last class and tell us about new assignment
// BMI Calculator
// Declare a variable 'weight' and assign it the value 70 (kg)
let weight = 70;
// Declare a variable 'height' and assign it the value 1.75 (meters)
let height = 1.75;
// Calculate BMI using the formula: BMI = weight / (height * height)
let bmi = weight / (height * height);
// Print the calculated BMI using a template literal
console.log(`Fayaz, Your BMI Is ${bmi}`); // Outputs: Fayaz, Your BMI Is 22.857142857142858
export {};
